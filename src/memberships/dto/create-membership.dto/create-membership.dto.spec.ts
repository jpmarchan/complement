import { CreateMembershipDto } from './create-membership.dto';

describe('CreateMembershipDto', () => {
  it('should be defined', () => {
    expect(new CreateMembershipDto()).toBeDefined();
  });
});
